name: Daily Holiday Check

on:
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: SUPER OBVIOUS TEST
        run: |
          echo "HELLO I AM THE NEW WORKFLOW"
          exit 1
